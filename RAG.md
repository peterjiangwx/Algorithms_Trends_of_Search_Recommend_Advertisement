<img width="758" height="793" alt="image" src="https://github.com/user-attachments/assets/dda76c0d-0a2e-49d2-8a05-1a693bd91e18" />  </br>
# 1、llm痛点  
- 幻觉：llm会自信地编造假信息
- 时效性：llm的训练数据决定了模型的知识范围，对于最新的事件llm不知晓
- 私有数据盲区：由于隐私的问题，一般llm不了解企业内部的文档知识

# 2、rag的工作流程
- 索引：将不同的数据源，切分成小的语义块，存储到向量数据库
- 检索：根据query，从索引中检索目标语义块
- 生成：利用检索结果，构造上下文，进行生成

# 3、朴素rag的局限  
- 召回不准：向量相似度无法完全捕捉语义相关性，尤其在专业领域
- 上下文割裂：文档被机械切分，丢失了全局逻辑和跨段落关联
- 静态知识：无法处理实时变化的数据，例如股票价格、库存状态
- 长文档理解弱：面对百页pdf，传统rag难以进行全局推理

# 4、怎么解决上述问题
- Adaptive RAG：自适应检索
  * 核心：根据问题的复杂度和类型，动态决定是否检索、检索多少次、检索什么数据源
  * 效果：减少不必要的api调用，降低成本，提升准确率
- Graph RAG：图检索增强
  * 核心：将知识库构建成知识图谱，而非简单的向量列表，实体和关系被显式建模
  * 优势：支持多跳推理（multi-hop reasoning）， 能回答“公司ceo的母校是哪所大学”这类链式推理的问题
 
- 全局感知RAG（MiA-RAG）
  * 核心：在检索前，先为整个长文档生成一个高层摘要（全局视图），左内容压缩
  * 作用：指导检索过程，带着全文的理解去寻找细节证据
- 实时流式RAG
  * 核心：通过监听数据库的变更日志，实现知识库的秒级更新
  * 场景：金融行情、物流状态、客服工单等动态数据场景
- 多模块RAG
  * 核心：检索和生成的对象不仅限于文本，还有图像、音频、视频
  * 架构：使用多模态嵌入模型，如CLIP，将不同模态数据映射到同一向量空间
  * 应用：医疗影像报告生成，电商商品多模态问答

# 5、rag优化  
- 数据预处理
  * 智能分块：使用语义边界（比如标题，段落）
  * 元数据注入：为每个文本块添加来源、作者、时间等元数据，供检索时过滤用
- 检索优化
  * 混合检索：结合向量检索与关键词检索
  * 重排序：用更强大的交叉编码模型对结果进行精排
- 生成与评估
  * 结构化输出
  * 评估指标：出准确率外，还关注忠实度，答案是否严格基于检索内容

===========================================================================
# 6、如何解决召回不准的问题  
召回不准是直接导致模型生成幻觉、答非所问的原因，在工业界，通产表现为 噪声过多（召回了很多无关结果）或者核心答案没检索出来，用搜索的语言来讲就是精度和召回的问题  
具体原因估计：语义不匹配、文档分块不当、索引质量差
需要全链路治理，而不仅仅靠更换更好的embedding模型，将整个优化流程分为：检索前、检索中、检索后以及数据治理四个阶段  
- 检索前优化：理解用户的真实意图
  * query rewrite：利用一个轻量级大模型，将原始query改写为更规范的rewrite_query
  * query decomposition：对一个复合query进行拆分
  * HyDE（虚拟文档嵌入）：让LLM根据用户的提问，利用llm自身知识生成一篇格式符合要求的虚拟答案，然后用该答案的embedding去向量库检索
    - 优点：解决问与答的非对称检索难题（用词分布、句式、字数的不同）、极强的知识泛化与对齐能力、零微调成本（基于prompt）
    - 缺点：成本增加、首字延迟、误导性检索（如果假设答案有偏，则导致召回的结果有误，进而影响最终生成的效果）
    - 改良：使用轻量模型、限制生成长度、混合检索兜底
    - 总结：类似对query进行了一些语义改写，在长尾、口语化、知识跨度大的检索场景下是提准神器，但在高并发、低延迟、事实性严苛的场景下，需要配合小模型和混合检索一起
- 检索中优化：即检索策略优化，混合检索和动态路由
  * Hybrid Search：结合向量检索和关键词匹配，既能利用向量检索的泛化能力，又能利用关键词匹配的精准命中能力
  * Sparse/Dense权重融合(RRF)：使用倒数排名融合等算法，将BM25的排名和Vector的排名进行非线性加权，输出综合召回列表，不过RRF只适合在粗排阶段使用，对多路召回结果进行排序，因为RRF融合分数只适合相对排序
- 检索后优化：对召回的结果进行精排，取topK结果给到大模型
  * cross-encoder rerank
    - 做法：利用Bi-Encoder快速捞出top 50候选chunk，然后用一个cross-encoder（如baai的bge-reranker，cohere Rerank，jina Reranker）对top50个候选chunk进行打分
  * context compression(上下文压缩)
    - 做法：很多chunk只有部分句子有意义，使用如LLMLingua等上下文压缩技术，剔除chunk中冗余的token，防止大模型发生 lost in the middle（注意力涣散）
- 数据治理（数据源和底层治理）
  * 元数据增强、标签化：对文档切块时，人工或者利用llm为每个chunk打上元数据标签
    - 应用：检索时，利用元数据进行硬过滤，排除掉非目标范围chunk
  * embedding微调：通用的开源embedding没有行业专有名词的概念和上下文知识，使用企业内部的行业专属语料，对embedding进行对比学习微调，让他在业务场景下提升能力
  * 分块策略优化
    - 固定长度优化：容易丢失语义块
    - 语义感知分块：按段落、句子边界、标题层级
    - 父子分块：大块提供上下文，小块用于精准检索，利用小块匹配到的结果返回大块内容
  * 索引增强
    - 问题-答案对（QA）索引：先对doc进行分块，利用llm对chunk进行问题生成，对问题进行embedding构建索引，payload指向原始chunk，检索时，通过问题匹配并返回原始chunk
    - 摘要索引：大文档-> LLm生成摘要->索引摘要，检索时，通过摘要匹配返回原文信息
    - 混合索引：QA索引+chunk索引，双路并行，检索时，一路与生成的问题匹配，一路与原文chunk进行匹配，最后通过RRF融合排序

总结：工业级标准组合拳  
**User Query -> HyDE -> 混合检索 -> MetaData过滤 -> Reranker -> LLM生成**

# 7、如何解决上下文割裂的问题
上下文割裂主要的原因还是在切分块的时候，把关联的语义模块给切分到了多个chunk中，导致只看见了部分信息，从而发生指代不清楚，缺少上下文信息的情况，目前工业界比较成熟的解决方案有以下四种范式  
- 父子分块策略（parent-child chunking）
  * 核心做法：父分块比较大，包含了子分块周围完整的上下文，子分块比较小，保持较高的语义密度，负责在向量检索时精准命中
- 分层摘要架构（Hierarchical Summary RAG）
  * 核心做法
    - 利用大模型（轻量级）对整篇长文章的每一段、每一章、每一节分别生成摘要
    - 将这些摘要组成一颗知识树，底层是原始的chunks，上一层是局部摘要，顶层是正本书的全局大纲
  * 用法：当用户问出全局性问题时，系统先检索顶层摘要树，确定哪些章节有关联，再深入到具体的chunk
- 动态滑动窗口与前后文拼接
  * 核心做法：在保存chunk到向量数据库是，不进存当前文本，还为其绑定前后两个相邻的chunkid或部分文本（overlap）
  * 用法：在匹配到具体chunk-25的情况下，在拼接给大模型前，工程自动从数据库中捞出chunk-24和chunk-26，拼接在一起给到大模型
- GraphRAG（知识图谱增强RAG-终极解法）
  * 核心做法：在预处理阶段，利用LLM扫描所有切片，提取出文本中所有的实体和关系，在数据库中建立一张巨大的知识图谱
- Contextual Chunking，给每个块注入上下文
  * anthropic官方提出的方案，每个chunk存储前都加入它在原文中的位置和背景说明，比如来自什么文章，基于整体文章，该chunk说明了什么，利用大模型进行生成，然后拼接到原始chunk前面存储到向量数据库
总结：推荐组合方案
**文档入库：contextual chunking+父子分块， 检索阶段：混合检索+命中后扩展上下文窗口，最后进入LLM生成答案**

# 8、如何解决高频动态数据问题
通过检索向量数据库的方式没办法解决该问题，需要升级到Agentic RAG
- 外部工具调用
  * LLM通过对query进行识别，并提取出结构化参数
  * 动态路由：系统根据意图，放弃向量数据库，触发一个绑定好的工具（比如python函数）
  * 即时注入：从接口获取json数据
  * 组装：系统将结果组装进prompt中，给到llm

# 9、如何解决长文档理解弱的问题
- 升级检索机制：从单次捞取到多跳与智能代理
  * Agentic Multi-Hop Retrieval：引入大模型的推理链，如ReAct框架，自动识别是否要生成query并进行检索，经过多轮后，把完整信息给到大模型
  * Parent-Child Chunking：检索的时候用小分块，给到大模型的是大分块
- 精细化上下文治理：从堆砌文本升级为信息提纯
  * 交叉重排：检索出多个片段，然后利用cross-encoder进行重排，保留下最相关的top5片段，从而避免lost in the middle（注意力涣散）
  * 上下文压缩：通过一个小模型，将片段进行压缩之后再给到大模型，解决注意力涣散的问题
