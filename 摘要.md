
文本摘要：  
* 基于词权重的方式：比如tfidf，计算词权重，抽取权重较高的词作为摘要
* 基于序列标注的方式进行抽取：比如crf模型
* 基于分类的方法进行抽取：对doc的句子进行分类，判断句子是否可以作为摘要的一部分，然后拼接作为摘要
* 基于生成的方式：比如pointer-generator，既考虑抽取，也考虑生成，还能缓解句子重复的问题
* 基于大模型生成的方式：基于大模型是后面的趋势  
